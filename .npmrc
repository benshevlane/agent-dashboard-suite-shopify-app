# The @vercel/remix preset pins an exact @remix-run/node peer; the Remix
# packages float within 2.x. They are compatible, so relax peer resolution
# (the fix npm's ERESOLVE message recommends).
legacy-peer-deps=true
